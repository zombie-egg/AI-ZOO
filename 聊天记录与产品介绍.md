# AI 动物合照机：聊天记录、产品介绍与接手说明

> 最后更新：2026-09-02  
> 当前主方案：V4 无模板直连。以后接手本项目时，以本文和 `GPT_IMAGE_2_DIRECT_V4.md` 为准；`docs/PHASE1_*` 是早期底座验收记录，不代表现在的产品流程。

## 一、我们最后确认了什么

项目最初从 `luna-swapping` 开源底座改造而来。早期方案曾经考虑“固定动物模板 + 本地换脸预览 + 付款后 GPT 精修”，实际生成后发现人物像被硬贴进模板，衣服、姿势、光影和动物关系很不自然。因此项目所有者最终明确决定：

1. 不再生成、选择或使用任何模板图。
2. 不再提供所谓免费本地预览，也不允许失败时回退成假图。
3. 顾客先用文字选择“想和谁、在什么场景合照”。
4. 每个文字选项在服务端对应一份固定、版本化的完整长 Prompt。
5. 现场相机拍同一位顾客四张原图，按固定顺序全部发给 `gpt-image-2`。
6. `gpt-image-2` 直接生成一张全新的动物合照。
7. 成品必须人工大图确认；确认前不能打印。
8. 本机测试可以跳过支付，但仍然是真实付费 API 调用，不能伪装成本地演示。
9. 成品图片本体及打印画面不叠加“AI 生成”角标或其他文字。

## 二、产品是什么

这是面向动物园、景区或线下活动现场的自助拍照设备：

- 游客在大屏上阅读人脸照片授权；
- 选择熊猫、小熊猫、大象、长颈鹿、火烈鸟、海豚、狐猴、白虎或水豚场景；
- 相机依次拍摄身体/穿搭基准、正脸、左侧三分之二、右侧三分之二，共四组照片；
- 每组用三帧连拍自动选择较清晰的一帧；
- 服务端把四张原图和选中场景的固定长 Prompt 直接发给 `gpt-image-2`；
- 页面展示高清候选图，工作人员或游客检查本人相似度、衣服、手部、动物、空间关系和光影；
- 人工确认后才生成打印任务；Canon SELPHY CP1500 未连接时只交付电子版，不假装已经打印。

## 三、真实性原则

这是产品的第一优先级，Prompt 已把以下内容写成硬约束：

- 五官、脸型、肤色、年龄感和辨识特征必须像同一个真人；
- 保留发型、眼镜、配饰、身材比例和真实穿搭；
- 禁止美颜、网红脸、改年龄、改性别、改族裔和过度磨皮；
- 人物肤色要接收场景的环境反光，但不能改变原本肤色；
- 人、动物、玻璃、栏杆、地面必须共享同一光源、阴影方向和空间；
- 使用游客自拍或朋友手机随手拍质感，而不是影楼写真或旅游宣传海报；
- 允许轻微高 ISO 噪点、运动柔化、构图不完美等真实相机痕迹；
- 动物比例和解剖必须正确，互动必须符合真实动物园安全边界；
- 只允许一个人物，禁止多脸、重复人、畸形手、多余肢体、文字、水印和拼贴。

固定 Prompt 的唯一代码来源是：

- `imageforge/app/scene_catalog.py`

前端永远只传 `scene_id`，不能让浏览器拼 Prompt，也不能接受游客自由输入后直接注入模型。

## 四、四张照片的固定顺序

顺序不能排序、不能打乱：

1. `body_anchor`：身体、发型、眼镜、配饰和衣服基准；
2. `front_smile`：最主要的正脸身份参考；
3. `left_three_quarter`：左侧面部深度参考；
4. `right_three_quarter`：右侧面部深度参考。

ImageForge 会把四张图片作为重复的 `image[]` 字段提交到兼容 OpenAI Images Edits 的接口。不存在第五张模板图，也不存在多图不兼容时静默退回单图的逻辑。

## 五、当前代码结构

- `kiosk/`：Vue 3 + Vite 大屏端；场景选择、真实摄像头、四连拍、支付/本机测试、生成轮询、人工确认和打印交付。
- `server/`：ThinkPHP 业务层；订单、授权、场景、照片、支付、可靠出箱、生成状态、人工确认和打印状态。
- `imageforge/`：FastAPI 图像服务；图片质检、固定 Prompt 场景目录、四图直传、签名下载和打印排版。
- `server/database/phase3_kiosk.sql`：新库完整 Kiosk 表结构。
- `server/database/phase4_direct_generation.sql`：旧库升级到 V4 的可重复执行迁移。
- `GPT_IMAGE_2_DIRECT_V4.md`：详细架构、接口、Prompt 规则和验收标准。

旧模板 HTTP 路由、免费预览 API、本地换脸流水线、单图兼容回退和预览脚本均已从活动代码中删除。仓库忽略 `imageforge/templates/`，该目录里的历史本机素材不得提交或部署。

## 六、API 配置

项目继续使用所有者最开始提供的中转站：

- Base URL：`https://www.codexapis.com/v1`
- 模型：`gpt-image-2`

密钥只保存在 `imageforge/.env` 的服务端变量中，本文不记录明文。不要把密钥写进 Vue、Git、截图、日志或公开压缩包。接手者需要在自己的私密环境中配置：

```dotenv
GPT_IMAGE_BASE_URL=https://www.codexapis.com/v1
GPT_IMAGE_API_KEY=<私下获取>
GPT_IMAGE_MODEL=gpt-image-2
IMAGE_PROVIDER_OVERRIDE=
```

生产环境不要设置 `IMAGE_PROVIDER_OVERRIDE=mock`。

## 七、本机启动

先启动 Docker Desktop，然后在仓库根目录运行：

```powershell
docker compose -f docker-compose.local-real.yml up -d
Get-Content -Raw server\database\phase4_direct_generation.sql | docker compose -f docker-compose.local-real.yml exec -T mysql mysql -uroot -pphase1-root-local-only loxi_luna_test
```

启动 ImageForge：

```powershell
cd imageforge
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

另开终端启动 Kiosk：

```powershell
cd kiosk
npm install
npm run dev -- --host 127.0.0.1 --port 4175
```

打开：`http://127.0.0.1:4175/kiosk/`

本机 Docker 启动后会占用部分端口，因此开发端口已改为 `4175`。PHP API 在 `http://127.0.0.1:8080`，ImageForge 在 `http://127.0.0.1:8000`。

## 八、部署前必须改的开关

```dotenv
KIOSK_OPERATOR_TEST_MODE=false
VITE_KIOSK_LOCAL_OPERATOR_MODE=false
IMAGE_PROVIDER_OVERRIDE=
```

此外必须：

- 把所有 `change-*` 内部令牌换成高强度随机值，并保证 PHP 与 ImageForge 两端一致；
- `PUBLIC_BASE_URL` 改成公网 HTTPS 域名，否则电子版二维码仍会指向 localhost；
- 只允许 PHP 内网访问 `/v4/*` 和 `/internal/*`，浏览器不能拿到 ImageForge 内部令牌；
- 用 Nginx/对象存储保护原图和成品，禁止公开目录列举；
- 每分钟执行一次 `php think kiosk:outbox`，兜底处理支付成功但浏览器掉线的订单；
- 定时执行 `php think kiosk:cleanup`，按隐私策略删除过期照片；
- 打印电脑安装 Canon SELPHY CP1500 驱动并启动本地打印代理；
- 正式营业前接入真实 InsightFace/ArcFace 身份相似度引擎。身份引擎只做 QA，不能换脸、回锁或二次美颜；即使自动分数通过，也继续保留人工大图确认。

## 九、2026-09-02 实测结果

- ImageForge：7 项自动测试全部通过；
- Python：`compileall` 通过；
- Kiosk：Vite 生产构建通过；
- PHP：V4 涉及的控制器、Logic、服务、支付回调和 outbox 命令均通过 `php -l`；
- MySQL：V4 迁移已修正为 MySQL 8 可重复执行，并连续执行两遍验证；
- 中转站：`/models` 返回成功，`gpt-image-2` 可见；
- 真实生成：用同一位游客四张已授权照片进行一次熊猫场景调用，成功返回 `review_required`，没有单图回退，没有模板回退；
- 实测成品：人物五官、发型和深蓝色上衣得到保留，环境绿色反光、玻璃、栏杆和熊猫空间关系自然；任务停在人工确认，未自动打印。

本机实测任务 ID：`580e841e5f4d6bf2d2c36890`。成品仅保存在本机私有目录；其中包含真人照片，公开分享项目之前应先清理 `imageforge/data/` 和 `server/runtime/kiosk-private/`。

## 十、仍需在真实商业现场完成的事情

1. 用正式相机、真实灯光和 20–50 位授权测试者建立身份相似度基线；
2. 针对眼镜、刘海、深肤色、侧脸、逆光和宽松衣服做专项测试；
3. 给 9 个场景分别做至少 20 张抽样，统计脸像本人、手部、动物结构和空间关系失败率；
4. 接入微信真实支付回调、退款和运营后台人工重做；
5. 连接 CP1500，验证 100×148 mm、300 dpi、色彩和出血边距；
6. 做 HTTPS、限流、日志脱敏、备份恢复和密钥轮换；
7. 为 API 成本、耗时、失败率、人工驳回率设置监控报警。

## 十一、给下一个 Codex 的开工提示

先完整阅读本文、`GPT_IMAGE_2_DIRECT_V4.md`、`imageforge/app/scene_catalog.py` 和 `imageforge/app/pipelines.py`。任何新改动都必须守住三条底线：

1. 不恢复模板或免费假预览；
2. 不允许四图失败后静默降为单图；
3. 不允许自动身份分数替代人工成品确认。
